#include <bits/stdc++.h>

using namespace std;

int main( int argc, char ** argv ) {
//    freopen( "in.txt", "r", stdin );
//    freopen( "out.txt", "w", stdout );

    int n;
    while( scanf( "%d", &n )==1 && n != 42 ) {
        printf( "%d\n", n );
    }
    return 0;
}
