#include <bits/stdc++.h>

using namespace std;

int main( int argc, char ** argv ) {
    int M, N;
    scanf( "%d %d", &M, &N );

    int domino = (M * N) / 2;

    printf( "%d\n", domino );
    return 0;
}